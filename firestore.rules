rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access goes through the API (Firebase Admin SDK bypasses these rules)
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
