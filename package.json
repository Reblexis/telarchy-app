{
  "name": "metrics-tracker",
  "version": "1.0.0",
  "description": "Personal metrics tracking app with formulas and dependencies",
  "scripts": {
    "deploy": "npx --yes firebase-tools@latest deploy --only hosting --non-interactive"
  }
}

